import 'package:flutter/material.dart';

class AppConstants {
  const AppConstants._();

  static const appName = 'Bijak Belajar';
  static const privacyNote =
      'Tiny Finder does not collect personal information from children. '
      'The app works offline and stores learning progress only on this device.';

  static const pagePadding = EdgeInsets.symmetric(horizontal: 20, vertical: 16);
}
